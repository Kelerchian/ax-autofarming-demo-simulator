{
  "name": "simulation-server",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "tsc",
    "start": "npm run build && node dist/simulation-server/src/index",
    "clean": "rimraf dist",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@actyx/sdk": "0.5.9",
    "@actyx/machine-runner": "0.5.6",
    "@actyx/machine-check": "0.3.0"
  }
}
